tags:
  - Employees
parameters:
  - name: employee_index
    in: path
    required: true
    schema:
      type: integer

responses:
  200:
    description: "Successful operation"
  404:
    description: "Employee not found"
