tags:
  - Animals
parameters:
  - name: animal_index
    in: path
    required: true
    type: integer
    format: int64
responses:
  200:
    description: "Successful Release Animal"
  404:
    description: "Animal not found"
