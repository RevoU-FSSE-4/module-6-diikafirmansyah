tags:
  - Employe
parameters:
  - name: employe_index
    in: path
    required: true
    type: integer
    format: int64
responses:
  200:
    description: "Employee has been kick out"
  404:
    description: "Employee not found"